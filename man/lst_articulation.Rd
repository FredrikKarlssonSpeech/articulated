% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/superassp_integration.R
\name{lst_articulation}
\alias{lst_articulation}
\title{Comprehensive articulation feature set (lst_* style)}
\usage{
lst_articulation(
  audio_file,
  vsa_method = c("basic", "density", "continuous"),
  formant_args = list(),
  intensity_args = list(),
  return_tracks = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{audio_file}{Path to audio file}

\item{vsa_method}{Vowel space area method: "basic", "density", or "continuous"}

\item{formant_args}{List of arguments for formant extraction}

\item{intensity_args}{List of arguments for intensity extraction}

\item{return_tracks}{Include intermediate tracks in output? (default: FALSE)}

\item{verbose}{Print progress messages? (default: TRUE)}

\item{...}{Additional arguments passed to metric computation}
}
\value{
Named list of features with atomic values suitable for data frames
}
\description{
Computes a complete set of articulation features from an audio file,
returning a flat named list suitable for data analysis pipelines.
Compatible with superassp's lst_* function design.
}
\examples{
\dontrun{
library(superassp)
# Extract all features
features <- lst_articulation("speech.wav")

# Convert to data frame
df <- as.data.frame(features)

# Process multiple files
files <- list.files("audio/", pattern = "\\\\.wav$", full.names = TRUE)
results <- lapply(files, lst_articulation)
results_df <- do.call(rbind, lapply(results, as.data.frame))
}
}
\author{
Fredrik Nylén
}
