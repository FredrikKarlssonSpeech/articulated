% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/superassp_integration.R
\name{articulated_batch}
\alias{articulated_batch}
\title{Batch process multiple audio files}
\usage{
articulated_batch(
  audio_files,
  feature_fn = lst_articulation,
  parallel = FALSE,
  n_cores = parallel::detectCores() - 1,
  progress = TRUE,
  stop_on_error = FALSE,
  ...
)
}
\arguments{
\item{audio_files}{Character vector of audio file paths}

\item{feature_fn}{Feature extraction function (default: \code{lst_articulation})}

\item{parallel}{Use parallel processing? (default: FALSE)}

\item{n_cores}{Number of cores for parallel processing (default: all available - 1)}

\item{progress}{Show progress bar? (default: TRUE)}

\item{stop_on_error}{Stop if any file fails? (default: FALSE)}

\item{...}{Arguments passed to feature_fn}
}
\value{
Data frame with one row per file
}
\description{
Processes multiple audio files in parallel or sequentially, extracting
articulation features and returning results as a data frame.
}
\examples{
\dontrun{
files <- list.files("audio", pattern = "\\\\.wav$", full.names = TRUE)

# Sequential processing
results <- articulated_batch(files, lst_articulation)

# Parallel processing
results <- articulated_batch(files, lst_rhythm, parallel = TRUE, n_cores = 4)

# Save results
write.csv(results, "articulation_features.csv", row.names = FALSE)
}
}
\author{
Fredrik Nylén
}
